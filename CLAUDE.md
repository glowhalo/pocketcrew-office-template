# PocketCrew 사무실 저장소

이 저장소는 [PocketCrew](https://glowhalo.github.io/pocket-crew/app/) 앱이 만든 "AI 사무실"입니다.
이 저장소를 연 Claude Code 세션(당신)의 역할은 아래 절차를 그대로 따르는 것입니다 — 사람이 지시하지 않아도 매번 이 순서로 움직이세요.

## 세션이 열릴 때마다 할 일

1. `지시사항.md`를 읽는다. `상태.json`의 `hasPendingInstruction`이 `true`면, `지시사항.md`의 가장 마지막 항목(아직 "처리 완료" 표시가 없는 것)이 이번에 처리할 지시다.
2. 그 지시를 실제로 수행한다 — 조사·작성·정리 등 지시 내용에 맞는 결과물을 이 저장소 안에 실제로 만든다 (예: `output/` 폴더에 문서 생성). 결과물 없이 상태만 바꾸지 않는다.
3. 작업하는 동안 `상태.json`의 관련 직원 항목을 갱신한다 — `status`는 `"working"`(작업 중) 또는 `"idle"`(대기), `task`는 지금 하는 일을 짧은 한국어 문장으로, `updatedAt`은 현재 시각(ISO 8601). 이 파일은 뷰어 앱이 그대로 읽어 화면에 보여주므로 **구조(키 이름)는 바꾸지 말고 값만 갱신**한다.
4. 지시를 완료하면 `지시사항.md`의 그 항목에서 `- 상태: 대기 중`을 `- 상태: 처리 완료 (YYYY-MM-DD HH:MM)`으로 바꾸고 결과 한 줄 요약을 덧붙인 뒤, `상태.json`의 `hasPendingInstruction`을 `false`로 되돌린다. 이때 `상태.json`의 **최상위 `updatedAt`도 현재 시각으로 갱신**한다 — 뷰어가 "마지막 갱신"으로 보여주는 값이다.
5. 변경사항을 커밋하고 **반드시 원격에 푸시한다** (`git push`). 별도 지시가 없으면 바로 현재 브랜치에 커밋한다 (PR 필요 없음, 이 저장소는 사용자 1인 소유).

> ⚠️ **푸시하지 않으면 사용자에게는 아무 일도 일어나지 않은 것으로 보입니다.**
> 뷰어 앱은 GitHub API로 이 저장소를 읽는데, API는 **푸시된 커밋만** 볼 수 있습니다.
> 로컬에 커밋만 해두면 사용자 화면의 사무실은 영원히 멈춰 있습니다.
> 작업을 마쳤다면 푸시까지 끝내야 한 사이클이 완료됩니다.

## 직원 구성

`상태.json`의 `employees` 배열이 이 사무실의 직원 명단이다. 각 직원은 `role`(역할)을 가지고 있다 — 지시가 특정 직원을 지목하지 않으면, 지시 내용에 가장 맞는 역할의 직원이 맡아 처리하고 그 직원의 상태를 갱신한다. 직원을 더 고용하고 싶다는 지시가 오면 `employees` 배열에 새 항목을 추가해도 된다(다만 뷰어가 알아볼 수 있게 `id`는 고유하게, `role`은 한글 역할명으로).

## 파일 안내

- `상태.json` — 뷰어 앱이 주기적으로 읽는 상태 파일. **키 구조는 고정**, 값만 갱신.
  ```jsonc
  {
    "updatedAt": "2026-09-07T04:00:00.000Z",  // 마지막으로 실제 작업한 시각 — 매번 갱신할 것
    "team": { "label": "팀 이름" },
    "employees": [
      {
        "id": "고유값",
        "role": "전략가",
        "status": "working|idle",       // 뷰어가 이걸로 대기/작업 그림을 바꾼다
        "task": "지금 하는 일(짧은 한국어)",
        "updatedAt": "ISO 8601",
        "roomId": "f1-office-xl",       // 어느 방에 있는가 (아래 목록 참고)
        "deskSlot": 0,                  // 그 방 몇 번째 자리인가 (생략하면 자동 배치)
        "characterId": "urban-01"       // 어떤 모습으로 그릴까 (아래 목록 참고)
      }
    ],
    "hasPendingInstruction": false
  }
  ```

  **`roomId`** — 방 이름은 **앱이 만든다.** `f{층}-{공간}` 형태다.
  1층은 아래 셋으로 고정이고, 바꿀 수 없다.

  | 값 | 방 | 자리 | 쓰임새 |
  |---|---|---|---|
  | `f1-office-xl` | 🏠 사무공간 (기본값) | 8 | 각자 일하는 넓은 업무 공간 |
  | `f1-meeting-s` | 🪑 회의실 | 2 | 둘이 마주 앉는 작은 자리 |
  | `f1-pantry-s` | 🍵 탕비실 | 2 | 책상 없이 벤치에 앉아 쉬는 곳 |

  자리보다 사람이 많으면 넘친 인원은 사라지지 않는다 — **빈자리가 있는 다른
  방으로 옮겨 앉히고** 그 사실을 화면에 알린다. 모든 방이 꽉 차면 그때는
  "자리가 모자라요"라고 표시된다. 방을 옮기고 싶으면 `roomId`만 바꾸면 된다.

  **`status`가 `"idle"`인 직원은 자리를 안 정해두면 쉬는 공간(탕비실·라운지·
  카페)에 앉는다.** 일부러 자리를 정해뒀다면 그 자리가 우선이다.

  **`floor`** — 사용자가 2층을 올리고 공간을 넣었다면, 직원을 그 층에 둘 수
  있다. 숫자로 적는다(`"floor": 2`). 안 적으면 1층이다. 없는 층을 적으면
  조용히 1층으로 떨어진다 — 화면이 깨지지는 않는다.

  ```jsonc
  { "id": "e1", "role": "리서처", "status": "idle", "task": "쉬는 중",
    "floor": 2, "roomId": "f2-cafe-m" }
  ```

  층과 공간은 **사용자가 앱에서 직접 고른다.** 여기서 새로 만들지 말고,
  이미 있는 것에만 직원을 배치한다.

  **`characterId`** — 아래 목록 중에서 고른다. 색을 자유롭게 조합하는 방식이
  아니라 **정해진 그림 중 하나를 고르는** 방식이다(그림은 무한 재배색이 안 된다).

  | 값 | 모습 |
  |---|---|
  | `urban-01` | 갈색 머리 · 초록 셔츠 |
  | `urban-02` | 갈색 머리 · 붉은 상의 |
  | `urban-03` | 밝은 머리 · 보라 상의 |
  | `urban-04` | 안전모 · 주황 조끼 |
  | `urban-05` | 밝은 피부 · 청회색 |
  | `urban-06` | 검은 머리 · 머리띠 |

  **모르는 값을 넣어도 화면이 깨지지 않는다.** 뷰어가 목록에 없는 `roomId`·
  `characterId`는 기본값으로 떨어뜨린다. `deskSlot`이 범위를 벗어나면 자동
  배치로 넘어가고, 자리보다 사람이 많으면 남는 사람 수를 화면에 알린다 —
  사람이 조용히 사라지지는 않는다.

  **옛 형식도 그대로 동작한다.** 예전에는 `skin`·`hair`·`body`에 색을 직접
  넣었다. 그 값만 있는 직원은 역할(또는 그 색)로 알맞은 캐릭터를 찾아 그린다 —
  **굳이 지울 필요 없고, 새로 쓸 때는 `characterId`를 쓰면 된다.**
- `지시사항.md` — 사용자가 뷰어에서 남긴 지시 로그. 새 지시는 파일 맨 아래에 추가된다.
- 그 외 이 저장소 안의 파일/폴더는 자유롭게 만들어 실제 작업 결과물을 담아도 된다.
